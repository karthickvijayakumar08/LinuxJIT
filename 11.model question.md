USER MANAGEMENT

  How to create the new user
  Create a user and assign them to multiple groups.
  Lock the user "admin2" and verify the status.
  Create a group "developers" and add 2 users into it.
  Change the shell of user "student" to /bin/bash.
  Delete a user and remove the home directory also.
 How to check the passwd status.
  How to check the last login history of users?

  Create a user named student1 and verify the primary group assigned to that user.
  Create a new group named projectA.
  Assign  projectA as the primary group for user student1.
  Add a second user called student2 and check their default primary group.
  Display all groups available in the system.
  Display the groups that student1 belongs to


VI

  Open a new file called notes.txt using vi editor.
  Insert the following three lines into the file:
Linux is powerful
vi is a text editor
Practice makes perfect
  Save the file and exit vi.
  Open the same file again and append one more line at the bottom.
  Delete the 2nd line from the file using vi command.
  Undo the last change you made.
  Redo the undone change


PERMISSION:

  Create a file and give only read permission to everyone.
  Remove execute permission for "others" on a script.
  Set file permissions to rw-rw-r-- using numeric mode.
  Set directory permissions to rwxr-x--- using symbolic mode.
  Change owner of a file to another user.
  Change group ownership of a directory.
  Create a file with permission 600 and verify.
  Create a directory and deny entry (remove x). Test accessing it.
  Set default umask to 022 and check new file permissions.
  Write the steps to share a folder so that only group members can access it.
. 


COMPRESS

  Create a zip file containing 3 text files.
  Extract a zip file and verify contents.
  Compress a file using gzip and then decompress it.
  Create a tar archive of a directory and view its contents.
  Create a .tar.gz file containing multiple directories.
  Extract only one file from a .tar.gz archive.
  Compare the file size before and after using gzip.
  Test compression ratio difference: zip vs gzip.
  Create a tar archive, then compress it with gzip manually.
  Extract a .tgz file and check permissions.

PROCESS

  Start a process in background and bring it to foreground.
  Show how to check the parent of a process (PPID).
  Change the nice value of a running process.
  List only your user’s processes.
  Show how to kill a process using name instead of PID.
  Display processes sorted by memory usage.

FILE LINKS

  Create a hard link for a file and verify inode numbers.
  Create a soft link for a directory and check how it behaves.
  Delete the original file and observe soft link behavior.
  Delete the original file and observe hard link behavior.
  Show how link count changes using ls -l.
  Create 2 hard links and verify all show same data.
  Show that editing the hard link changes the original content.
  Prove soft link stores the path of the original file.

RPM&YUM

  Install an rpm package using the rpm command.
  Remove an rpm package using the rpm -e option.
  Install a package using yum and verify installation.
  List available repositories using YUM.
  Search for a package using yum search.
  Download an rpm package without installing it.
  Verify rpm package files using rpm -V.
  Clean yum metadata and cache.

Cat

How to create the file using the cat command
How read read the file using cat command
How add the more lines in the using cat command
How to delete the cat file in the users.
